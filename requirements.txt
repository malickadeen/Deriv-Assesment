# AI ticket-classification evaluation pipeline
#
# LangChain 1.x with the OpenAI-compatible chat client is used to talk to
# GPT-4o-mini through OpenRouter. Pydantic v2 provides structured-output
# schemas; python-dotenv loads the API key from .env; pytest runs the tests.
langchain>=1.0,<2
langchain-openai>=1.0,<2
openai>=1.30
pydantic>=2.6,<3
python-dotenv>=1.0
pytest>=8.0
